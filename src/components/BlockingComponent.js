// src/components/Blocking.js
import React from 'react';

const Blocking = ({ onBlock }) => {
    return (
        <div>
            <h2>Blocking</h2>
            {/* Implement blocking functionality */}
        </div>
    );
};

export default Blocking;
