// src/components/Following.js
import React from 'react';

const Following = ({ followedPosts }) => {
    return (
        <div>
            <h2>Following</h2>
            {/* Display followed friends' posts */}
        </div>
    );
};

export default Following;
